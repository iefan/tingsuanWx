<!--pages/dstingsuan/index.wxml-->
<view class="userinfo">
  <!-- <text>Hello world</text>  
  <text>当前时间：{{time}}</text>
  <text>{{ a === 10? "变量 a 等于10": "变量 a 不等于10"}}</text>
  <view wx:if="{{length > 5}}"> 1 </view>
  <view wx:elif="{{length > 2}}"> 2 </view>
  <view wx:else> 3 </view> -->

  <!-- <view wx:for="{{array}}">
    {{index}}: {{item.message}}
  </view> -->
  <text style="color: red; font-size: 66rpx; margin-bottom:30rpx;">大 晟 听 算</text>
  <text style="color: black; font-size: 26rpx; margin-bottom:60rpx;">（100以内加减法听算练习，每次10题，每题5秒）</text>
  <view  wx:if="{{flag===0}}">
    <view wx:for="{{numberArray}}" wx:key="index">
      <view style="color: green; font-weight:bold; font-size: 40rpx; margin-bottom:30rpx;">第{{index+1}}题</view>
    </view>
  </view>

  <view wx:if="{{flag===1}}">
    <view wx:for="{{numberArray}}" wx:key="index">
      <view style="color: green; font-weight:bold; font-size: 40rpx; margin-bottom:30rpx;">
        <text >第{{index+1}}题</text>
        <text> ： </text>
        <text>{{item[0]}}{{item[1]}}{{item[2]}}{{item[3]}}{{item[4]}}</text>
      </view>
    </view>
  </view>
  

  <!-- <switch wx:for="{{objectArray}}" wx:key="unique" > {{item.id}} </switch>
  <button bindtap="switch"> Switch </button>
  <button bindtap="addToFront"> Add to the front </button> -->


  <!-- <switch wx:for="{{numberArray}}" wx:key="*this" > {{item}} </switch> -->
  <view id='userbutton'>
    <!-- <button id="button" bindtap="Genquestion">生成题目 </button> -->
    <button id="buttonNT" bindtap="StartListen" disabled="{{btnDisabled}}">{{start_next_text}} </button>
    <!-- <button id="button" bindtap="DisplayAnswer" disabled="{{ansDisabled}}">显示答案 </button> -->
  </view>

</view>