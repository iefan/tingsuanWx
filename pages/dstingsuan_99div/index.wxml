<!--pages/dstingsuan/index.wxml-->
<view class="userinfo">
  <!-- <text>Hello world</text>  
  <text>当前时间：{{time}}</text>
  <text>{{ a === 10? "变量 a 等于10": "变量 a 不等于10"}}</text>
  <view wx:if="{{length > 5}}"> 1 </view>
  <view wx:elif="{{length > 2}}"> 2 </view>
  <view wx:else> 3 </view> -->

  <!-- <view wx:for="{{array}}">
    {{index}}: {{item.message}}
  </view> -->
  <text style="color: red; font-size: 50rpx; margin-bottom:15rpx;">表内有余数除法</text>
  <text class="descinfo" >（表内有余数听算练习，每次20题）</text>

  <view class="slide_div">
    <text class="section__title">设置答题时间（秒）</text>
    <view class="slide_view">
      <slider  bindchange="slider3change" min="1" max="5" step="1" show-value backgroundColor="#ee1933" block-color="#2066fe" block-size="28" value='3'/>
    </view>
  </view>

  <view  wx:if="{{flag===0}}">
    <view class="table" style="color: green; font-weight:bold; font-size: 40rpx; margin-bottom:30rpx;"> 
      <view class="tr">
        <view class="td_blank_head">第00题   </view>
        <view class="td_blank_head">第00题   </view>
      </view>     
      <view class="tr" wx:for="{{numberArray}}" wx:key="index">
        <view class="td" >第{{2*index+1}}题</view>
        <view class="td" wx:if="{{2*(index+1)<=numberArray_all.length}}">第{{2*index+2}}题</view>
        <view class="td_blank" wx:else>第00题</view>
      </view>
    </view>
  </view>

  <view wx:if="{{flag===1}}">
    <view wx:for="{{numberArray}}" wx:key="index" class="table" style="color: green; font-weight:bold; font-size: 34rpx;">
      <view wx:if="{{index===0}}" class="tr">
        <view class="td_blank_head">第66题:00÷00=00...0</view>
        <view class="td_blank_head">第66题:00÷00=00...0</view>
      </view>
      <view wx:if="{{index<numberArray.length/2}}" class="tr" >
        <view class="td_ans">第{{2*(index+1)-1}}题:{{numberArray[2*index][0]}}{{numberArray[2*index][1]}}{{numberArray[2*index][2]}}{{numberArray[2*index][3]}}{{numberArray[2*index][4]}}...{{numberArray[2*index][5]}}</view>
        <view class="td_ans">第{{2*(index+1)}}题:{{numberArray[2*index+1][0]}}{{numberArray[2*index+1][1]}}{{numberArray[2*index+1][2]}}{{numberArray[2*index+1][3]}}{{numberArray[2*index+1][4]}}...{{numberArray[2*index+1][5]}}</view>
      </view>
    </view>
  </view>
  

  <!-- <switch wx:for="{{objectArray}}" wx:key="unique" > {{item.id}} </switch>
  <button bindtap="switch"> Switch </button>
  <button bindtap="addToFront"> Add to the front </button> -->


  <!-- <switch wx:for="{{numberArray}}" wx:key="*this" > {{item}} </switch> -->
  <view id='userbutton'>
    <!-- <button id="button" bindtap="Genquestion">生成题目 </button> -->
    <button id="buttonNT" bindtap="StartListen" disabled="{{btnDisabled}}">{{start_next_text}} </button>
  </view>

</view>